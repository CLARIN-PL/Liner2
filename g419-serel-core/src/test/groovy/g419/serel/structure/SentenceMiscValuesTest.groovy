package g419.serel.structure

import g419.corpus.structure.Sentence
import spock.lang.Specification
import spock.lang.Unroll

class SentenceMiscValuesTest extends Specification {


   // SentenceMiscValues smv =
/*
    @Unroll
    def "#re isRuleElementMatchingSerel( #se )  should return #result"() {
        given:
        def  rmr =  RuleMatchingRelations.understandRule(re)
        when:
        def res = rmr.isRuleElementMatchingSerel(2,se)
        then:
        res == result
        where:
        re  | se   || result
        " location:: [nam_fac_goe] (root) :source < kompleks(appos) < miasto(nmod) < [nam_loc_gpe_city] (nmod) :target "  | " [nam_fac_goe](root)<kompleks(appos)<miasto(nmod)<[nam_loc_gpe_city](nmod) "  || true
        " location:: [nam_fac_goe] (root) :source < kompleks(appos) < miasto(nmod) < [nam_loc_gpe_city] (nmod) :target "  | " [nam_fac_goe](root)<kompleks(appos)<miasto(appos)<[nam_loc_gpe_city](nmod) "  || false


    }
*/




}


