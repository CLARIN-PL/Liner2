    Copyright (C) 2012 Michał Marcińczuk, Maciej Janicki
    Wrocław University of Technology  
    
Liner2 packages requires CRF++ (http://crfpp.googlecode.com/svn/trunk/doc/index.html).
The compatible version can be also found in the ./external folder.

-----------------------------
Step 1: Installation of CRF++
-----------------------------

If you have already have installed CRF++ go to step 2. If not, then continue this step.

CRF++ requires gcc and g++ to compile.

Navigate to liner2/external folder and unpack CRF++-0.57.tar.gz

  tar -xvf CRF++-0.57.tar.gz
  
Go inside CRF++-0.57 folder, compile and install the library:

  cd CRF++-0.57
  ./configure
  make
  sudo make install
  sudo ldconfig


-----------------------------
Step 2: Create Java binding
-----------------------------

Java bindig required java-jdk to compile.

Navigate to liner2/external/CRF++-0.57 (or your copy of CRF++ sources) and execute:

  cd java
  make

At this step you might encounter a problem with finding jni.h file. You have to
check a path to java-sdk, parameter INCLUDE in 'external/crfpp/java/Makefile'. 

Make sure that the files 'lib/libCRFPP.so' and 'lib/CRFPP.jar' were created.

Copy the above files to liner2/libs folder:

  cp libCRFPP.so ../../../lib
  cp CRFPP.jar ../../../lib


-----------------------------
Step 3: Compile liner2
-----------------------------

The Ant Apache tool is required to build liner2.jar package.

Execute the following command:

  ant jar
 
 
-----------------------------
Step 4: NERD dependencies
-----------------------------

Liner2 uses an external tool for features construction called NERD. It requires
following Python packages:
 * numpy        1.5.1    (python-numpy)
 * PyYAML       3.09     (python-yaml)
 * networkx     0.99     (python-networkx)
 * biopython    1.54b    (python-biopython)
 * setuptools   0.6c11   (python-setuptools)
 * MySQL-python 1.2.3    (python-mysqldb)
 * nltk         2.0.1rc1 (python-nltk)
 * Django       1.3      (python-django)

To check what packages are missing, please run following command:

  ./nerd/check.py


-----------------------------
Step 5: Test
-----------------------------
  
To check, if the configuration was successful run the following command:

  ./liner2.sh pipe -ini model-5nam-v1/model.ini -i ccl -f test/sentence.xml -o tuples
  
You should see following output:

	(4,6,PERSON_FIRST_NAM,"Ala")
	(7,11,PERSON_LAST_NAM,"Nowak")
	(20,28,CITY_NAM,"Warszawie")
